name: PSC1 root 100015 record extract

on:
  workflow_dispatch:
  push:
    branches: [diagnostic/psc1-root-100015-progress]
    paths:
      - ".github/workflows/psc1-root-100015-record.yml"

permissions:
  contents: read

jobs:
  extract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned Lean 4.34
        shell: bash
        run: |
          set -euo pipefail
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain leanprover/lean4:v4.34.0
          lean_bin="$("$HOME/.elan/bin/elan" which lean)"
          echo "LEAN434_BIN=$(dirname "$lean_bin")" >> "$GITHUB_ENV"
          echo "$(dirname "$lean_bin")" >> "$GITHUB_PATH"
          test "$("$lean_bin" --githash)" = "293d5d0c0c3f3dded4688b3ccd6a33939ac5102b"
      - name: Export and print stall records
        shell: bash
        run: |
          set -euo pipefail
          lean oracle/replay-probe/DependencyExport.lean
          lean --run oracle/replay-probe/DependencyExport.lean Std --root-range 100015 1 > /tmp/root100015.ndjson
          echo "bytes=$(wc -c < /tmp/root100015.ndjson)"
          echo "lines=$(wc -l < /tmp/root100015.ndjson)"
          echo "=== around former expr probe ==="
          nl -ba /tmp/root100015.ndjson | sed -n '190655,190665p'
          echo "=== around Array.push_eq_append ==="
          nl -ba /tmp/root100015.ndjson | sed -n '191655,191670p'
          echo "=== proof head / body records ==="
          grep -n -E '"ie":(103435|173305)|"ie":17331[4-8]' /tmp/root100015.ndjson || true
