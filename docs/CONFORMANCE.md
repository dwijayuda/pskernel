
## Lean 4.34 primitive-closure oracle

`npm run oracle:primitive` replays the pinned `oracle/fixtures/lean434-primitive-closure.ndjson` stream through the production importer and kernel. The fixture contains 15,780 export records (12,931 expressions, 348 declaration records) and must result in exactly 390 environment constants. It exercises the real Lean 4.34 dependency closure through `Nat.mod`, `Nat.div`, `Nat.gcd`, and `Nat.bitwise`; no exported primitive declaration is trusted without its production recognizer.

Private Lean names remain structural: `_private.Init.Data.Nat.Bitwise.Basic.0...` contains a numeric `Name.num 0` component and is intentionally not interchangeable with a dotted string component `"0"`.

## Lean 4.34 definitional-equality differential oracle

`npm run oracle:defeq` evaluates the same focused equality/non-equality cases with both the official Lean 4.34.0 toolchain and the TypeScript kernel. The current bounded set passes **24/24** paired cases. Coverage includes beta/zeta/delta behavior, theorem/opaque non-unfolding, function and structure eta, proof irrelevance, projection lazy-delta, Nat and Bool reduction, Nat recursor iota, unit-like equality, Unicode String expansion, quotient lift reduction, and universe `max`/`imax` normalization, plus negative Nat/String/lambda/eta cases.

This is a differential baseline, not an exhaustive equivalence proof; the global differential-oracle gate remains open until substantially broader generated/adversarial coverage exists.

## Lean 4.34 Init.Prelude corpus oracle

`npm run oracle:init` replays the pinned `oracle/fixtures/lean434-init-prelude.ndjson` dependency-aware export through the production importer and TypeScript kernel. The fixture is generated from the locally bootstrapped Lean 4.34.0 Release environment by `oracle/replay-probe/FullExport.lean`, following Lean's dependency-aware replay strategy rather than raw environment map order.

Pinned corpus invariants:

- 70,484 NDJSON records
- 8,295 interned names
- 115 universe-level records
- 60,035 expression records
- 2,038 declaration records
- exactly 2,324 admitted/generated constants

The corpus covers the full `Init.Prelude` imported environment and exercises real ordinary, mutual, indexed and nested inductives, auxiliary nested recursors such as `Lean.Syntax.rec_1`/`rec_2`, quotient initialization, primitives, definitions, theorems and opaque declarations. Exported generated metadata is compared against metadata regenerated by the TypeScript kernel; it is not trusted as admission authority.

`npm run check:corpus` runs the regular unit/anti-drift/oracle stack and then this full Init.Prelude replay.

## Bounded Std corpus

Pinned `Std.Internal.Parsec.Basic` dependency-closure roots replay:

- 23,239 NDJSON records
- 3,772 interned names
- 30 universe-level records
- 18,921 expression records
- 515 declaration records
- exactly 646 admitted/generated constants

This bounded corpus covers parser inductives, typeclass declarations, higher-order definitions, and partial recursion while keeping replay time suitable for a hard CI gate.

### Std.Sat.CNF.Basic bounded corpus

Pinned dependency-closure roots from `Std.Sat.CNF.Basic` replay through the same production importer/kernel:

- 11,338 NDJSON records
- 1,850 interned names
- 29 universe-level records
- 9,183 expression records
- 275 declaration records
- exactly 366 admitted/generated constants

The root set covers the `CNF` structure, clause/formula evaluation, construction (`empty`, `add`, `append`), proposition-valued `Sat`/`Unsat`, and clause/formula/variable membership predicates. It intentionally complements the Parsec corpus with array/list structures and logical membership/evaluation code rather than parser control flow.

`npm run oracle:std-sat-cnf` runs this gate independently; `npm run check:corpus` includes both bounded Std gates.

### Std.Data.ByteSlice bounded corpus

Pinned dependency-closure roots from `Std.Data.ByteSlice` replay through the production importer/kernel:

- 90,125 NDJSON records
- 9,531 interned names
- 45 universe-level records
- 79,106 expression records
- 1,442 declaration records
- exactly 1,630 admitted/generated constants

The root set covers `ByteSliceData`, byte-array/start/stop projections, size and indexed `get`, conversion to/from `ByteArray`, slicing, and recursive byte containment. It stresses `UInt8`/`ByteArray`, `Fin`-indexed access, proof-carrying slice bounds, and structure projections in a substantially different environment from the parser and SAT/CNF gates.

`npm run oracle:std-byteslice` runs this corpus independently. It is included in `npm run check:corpus`, while short development loops can run the individual corpus commands separately.

## Bounded Lean corpus: Lean.Data.RBMap

Pinned dependency-closure roots from `Lean.Data.RBMap` replay through the production importer/kernel:

- 41,107 NDJSON records
- 5,039 interned names
- 38 universe-level records
- 35,396 expression records
- 633 declaration records
- exactly 743 admitted/generated constants

The root set covers `Lean.RBColor`, the dependent `Lean.RBNode α β`, recursive depth/fold/singleton operations, red-black balancing, insertion, and the well-founded `appendTrees` definition. This is the first persistent corpus gate rooted in the `Lean.*` implementation namespace rather than `Init` or `Std`.

The fixture is generated by the generic `oracle/replay-probe/DependencyExport.lean` dependency-closure exporter. That exporter has an explicit Lean-4.34 semantic-dependency hook for kernel-recognized primitives whose checker support declarations are not necessarily present in their syntactic used-constant set. This prevents a bounded dependency closure from accidentally omitting trusted-kernel prerequisites such as the generated `Nat.div.go` helper.

`npm run oracle:lean-rbmap` runs this gate independently and `npm run check:corpus` includes it.

## Adversarial soundness baseline

`npm run oracle:adversarial` runs a bounded set of Lean 4.34's own kernel regression files against the pinned official toolchain:

- `kernelProjIdx.lean` — oversized projection indices must not truncate into valid fields.
- `kernelProjSname.lean` — projection reduction must respect the declared structure family.
- `kernelNestedAuxName.lean` — user declarations may not inject the kernel's reserved `_nested` auxiliaries.
- `kernelMutualDupName.lean` — duplicate names inside a mutual definition block must be rejected transactionally.
- `kernelImaxPropInductive.lean` — `Sort (imax 1 0)` must normalize to `Prop` for inductive-elimination decisions.
- `kernel1.lean` / `kernel2.lean` — large Nat/Bool reduction, String/Char reduction, and non-reducible partial recursion behavior stay pinned to the official kernel. The final 4.34 tests no longer contain the removed `Lean.reduceNat` / `Lean.reduceBool` cases.
- `kernelImaxProp.lean` — normalized-Prop structures remain proof-irrelevant and cannot leak data through projections.
- `kernelMaxRecDepth.lean` — recursion-depth exhaustion is deterministic and succeeds when the explicit budget is raised.
- `kernelErrorFollowup.lean` — rejected declarations containing metavariables do not corrupt subsequent kernel interaction.
- `kernelBacktrack.lean` — kernel errors produced during failed elaboration branches are backtracked rather than poisoning successful alternatives.

The oracle currently passes **13/13** pinned Lean 4.34 kernel regression files. The TypeScript suite contains corresponding direct kernel regressions for the core bug classes, including wrong-family projection reduction, uint32 projection bounds, `_nested` namespace rejection, duplicate mutual-definition names, imax-normalized Prop elimination, and deterministic `maxRecDepth` rejection/success. This gate is still a baseline rather than a claim that Lean's full historical soundness regression corpus has been ported.

### Defeq cache soundness regressions

The adversarial oracle also runs Lean 4.34's `kernel_is_def_eq_equiv_manager_1.lean` and `kernel_is_def_eq_equiv_manager_2.lean`. These regressions protect the 4.34 change away from an equivalence-closure/union-find cache for algorithmic definitional equality. The TypeScript kernel mirrors that design with symmetric positive/negative expression-pair caches only; a unit regression explicitly prevents transitive closure from being introduced into the success cache.

## Large Lean corpus: full Lean.Data.RBMap environment

`npm run oracle:lean-rbmap-full` replays every constant in the imported `Lean.Data.RBMap` environment, exported in dependency-safe order by `oracle/replay-probe/DependencyExport.lean --all`. This is intentionally a separate large gate rather than part of the short `check:corpus` loop.

Pinned invariants:

- 463,440 NDJSON records
- 61,310 interned names
- 283 universe-level records
- 391,273 expression records
- 10,573 declaration records
- exactly 11,223 admitted/generated constants

The fixture is stored compressed as `oracle/fixtures/lean434-lean-rbmap-all.ndjson.gz`. On the identity-keyed kernel-cache architecture, the replay completes within the bounded local oracle window; this gate is primarily for large-environment compatibility and replay-performance regression detection.

`npm run check:large-corpus` runs this gate independently. It is deliberately excluded from `npm run check:corpus` so fast development cycles do not inherit the large-environment runtime and memory cost.

## Bounded Lean corpus: Lean.Data.PersistentArray

Pinned dependency-closure roots from `Lean.Data.PersistentArray` replay through the production importer/kernel:

- 85,105 NDJSON records
- 9,448 interned names
- 46 universe-level records
- 74,170 expression records
- 1,440 declaration records
- exactly 1,633 admitted/generated constants

The root set covers `Lean.PersistentArrayNode`, `Lean.PersistentArray`, empty/isEmpty, indexed lookup/update, push/pop, conversion, append, list conversion, and statistics. It adds nested recursion through `Array`, proof-carrying bounds, and persistent-array update code to the bounded Lean corpus surface.

This corpus exposed a Lean 4.34 nested-inductive parity bug: the TypeScript implementation reused the inductive header's parameter BinderInfo while transforming/restoring constructor types. Lean recreates the shared parameters from each constructor specifically to preserve constructor-local binder annotations. The TypeScript nested-inductive pipeline now mirrors that behavior during both preprocessing and restoration, while rebasing canonical nested-family parameters to the fresh constructor/restoration locals.

Reproducible export uses `oracle/replay-probe/DependencyExport.lean` with module `Lean.Data.PersistentArray` and roots `Lean.PersistentArrayNode`, `Lean.PersistentArray`, `Lean.PersistentArray.empty`, `Lean.PersistentArray.isEmpty`, `Lean.PersistentArray.get!`, `Lean.PersistentArray.set`, `Lean.PersistentArray.push`, `Lean.PersistentArray.pop`, `Lean.PersistentArray.toArray`, `Lean.PersistentArray.append`, `Lean.PersistentArray.toList`, and `Lean.PersistentArray.stats`.

`npm run oracle:lean-persistentarray` runs this gate independently; it is also included in `npm run check:corpus`.

## Bounded Lean corpus: Lean.Data.PersistentHashMap

Pinned dependency-closure roots from `Lean.Data.PersistentHashMap` replay through the production importer/kernel:

- 12,885 NDJSON records
- 2,177 interned names
- 32 universe-level records
- 10,377 expression records
- 298 declaration records
- exactly 397 admitted/generated constants

The root set covers `Lean.PersistentHashMap.Entry`, `Lean.PersistentHashMap.Node`, the public `PersistentHashMap` structure, `empty`/`isEmpty`, insertion, lookup (`find?` / `findEntry?`), `contains`, and unary-node recognition. It complements PersistentArray with nested bucket/node families, `BEq`/`Hashable` instance parameters, collision handling, and hash-trie recursive updates.

Reproducible export uses `oracle/replay-probe/DependencyExport.lean` with module `Lean.Data.PersistentHashMap` and the roots listed above. `npm run oracle:lean-persistenthashmap` runs this gate independently and it is included in `npm run check:corpus`.

## Native reduction: explicit TCB extension

Final Lean 4.34 **removed** the deprecated in-kernel native-reduction feature. Commit `2a7175c74ba17b160299accd7e6ea6984d7ea86f` removes `Lean.reduceBool`, `Lean.reduceNat`, `Lean.ofReduceBool`, `Lean.ofReduceNat`, and `Lean.trustCompiler` from `Init.Core`, removes compiler-IR execution from the C++ kernel, and removes the corresponding Meta WHNF/defeq hooks.

pskernel therefore does not expose a NativeEvaluator compatibility hook for the 4.34 profile. Native proof tactics such as `native_decide` are frontend/meta facilities that assert/check results through their own trust boundary; they are not kernel definitional reduction.

The official defeq differential excludes the obsolete pre-final native-reduction cases rather than synthesizing declarations that do not exist in the final 4.34 environment.



## Expanded adversarial soundness baseline

The direct TypeScript suite passes **97/97** cases. In addition to corpus and differential checks, it explicitly regression-locks unresolved expression/universe metavariable rejection and transactional rollback for duplicate mutual-inductive names.

The official Lean 4.34 adversarial oracle passes **21 expected-success + 1 expected-rejection** kernel regression files. This includes projection metadata checks, reserved nested names, duplicate mutual declarations, imax/Prop elimination behavior, non-transitive algorithmic-defeq cache regressions, recursion/heartbeat limits, kernel error recovery, proposition detection, string-cost behavior, Fin fold kernel reduction, and rejection of the Quot-name-collision exploit.


## Arena static correctness

An external Lean Kernel Arena corpus runner is available via `scripts/arena-static-oracle.mjs`. The current verified non-performance sweep is **170/170** expected verdicts. This gate found and regression-locked two issues: checking the structure argument during projection typing, and supporting valid sparse/out-of-order lean4export intern IDs. Performance benchmarks are tracked separately from semantic correctness.

## Full Std release gate

The preferred exhaustive Std path is the canonical module-order stream (`npm run oracle:std-full`), which checks each declaration once into a shared environment and discards replay-local intern tables/caches between module shards. Dependency-closure batching remains diagnostic tooling for isolating failures, not the primary release-completion definition.


### Deep-term runtime hardening

Deep structural traversals (`hasMVar`, `lift`, and `instantiate`) use explicit work stacks rather than the JavaScript call stack. A 6,000-binder regression is part of the normal suite. Heavy Arena/Full-Std oracle workers also run with an enlarged JavaScript stack, while the kernel's explicit `maxRecDepth` remains the semantic recursion guard.
