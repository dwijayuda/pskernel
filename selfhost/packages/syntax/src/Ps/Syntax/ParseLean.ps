import Ps.Syntax.Lexer
import Ps.Syntax.ParseCommon

inductive PsLeanFrontendError where {
  | lex(error : PsLexError);
  | parse(error : PsParseError);
};

/* During the Lean-first bootstrap, ParseLean.lean is authoritative.
   This sibling file marks the intended package/API surface and will be
   regenerated canonically when the PSC1 parser can express this slice. */
